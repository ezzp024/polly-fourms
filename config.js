window.POLLY_CONFIG = {
  // Leave blank to run in demo mode (local browser storage only).
  // Fill these from Supabase project settings for real multi-user forum data.
  supabaseUrl: "",
  supabaseAnonKey: "",

  // Nicknames with moderation tools access.
  moderatorNames: ["admin"],

  // Nicknames with full admin label (currently same permissions as moderator).
  adminNames: ["admin"]
};
